# LLMScout — bring your own LLM key (optional)
# Copy this file to .env and paste one key. Used by the AI narrative feature (v1.1).
# .env is gitignored — your key stays on your machine.

LLMSCOUT_LLM_API_KEY=
# or use a provider-specific variable instead:
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
